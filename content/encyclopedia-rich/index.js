const { ENTRIES: BATCH_1 } = require('./batch-1');

let BATCH_2 = [];
let BATCH_3 = [];
let BATCH_4 = [];
let BATCH_5 = [];
let BATCH_6 = [];
let BATCH_7 = [];
let BATCH_8 = [];
let BATCH_9 = [];
let BATCH_10 = [];
let BATCH_11 = [];
let BATCH_12 = [];
let BATCH_13 = [];
let BATCH_14 = [];
let BATCH_15 = [];
let BATCH_16 = [];
let BATCH_17 = [];
let BATCH_18 = [];
let BATCH_19 = [];
let BATCH_20 = [];
let BATCH_21 = [];
let BATCH_22 = [];
try {
  ({ ENTRIES: BATCH_2 } = require('./batch-2'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_3 } = require('./batch-3'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_4 } = require('./batch-4'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_5 } = require('./batch-5'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_6 } = require('./batch-6'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_7 } = require('./batch-7'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_8 } = require('./batch-8'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_9 } = require('./batch-9'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_10 } = require('./batch-10'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_11 } = require('./batch-11'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_12 } = require('./batch-12'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_13 } = require('./batch-13'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_14 } = require('./batch-14'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_15 } = require('./batch-15'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_16 } = require('./batch-16'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_17 } = require('./batch-17'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_18 } = require('./batch-18'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_19 } = require('./batch-19'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_20 } = require('./batch-20'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_21 } = require('./batch-21'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

try {
  ({ ENTRIES: BATCH_22 } = require('./batch-22'));
} catch (error) {
  if (error.code !== 'MODULE_NOT_FOUND') throw error;
}

module.exports = {
  ENTRIES: [
    ...BATCH_1,
    ...BATCH_2,
    ...BATCH_3,
    ...BATCH_4,
    ...BATCH_5,
    ...BATCH_6,
    ...BATCH_7,
    ...BATCH_8,
    ...BATCH_9,
    ...BATCH_10,
    ...BATCH_11,
    ...BATCH_12,
    ...BATCH_13,
    ...BATCH_14,
    ...BATCH_15,
    ...BATCH_16,
    ...BATCH_17,
    ...BATCH_18,
    ...BATCH_19,
    ...BATCH_20,
    ...BATCH_21,
    ...BATCH_22,
  ],
};
